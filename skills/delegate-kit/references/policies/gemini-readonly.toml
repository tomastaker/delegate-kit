# Restrict read-only workers to inspection tools. Shell checks belong to the coordinator.
[[rule]]
toolName = "*"
decision = "deny"
priority = 900

[[rule]]
toolName = ["read_file", "read_many_files", "list_directory", "glob", "grep_search", "google_web_search", "web_fetch"]
decision = "allow"
priority = 999
